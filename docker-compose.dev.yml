services:
  mongodb:
    image: mongo:7
    container_name: new-infra-test-mongodb-dev
    restart: unless-stopped
    volumes:
      - mongodb-data-dev:/data/db
    networks:
      - app-network
    healthcheck:
      test: ["CMD", "mongosh", "--eval", "db.adminCommand('ping')"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 30s

  server:
    build:
      context: .
      dockerfile: ./packages/server/Dockerfile.dev
    container_name: new-infra-test-server-dev
    ports:
      - "3001:3001"
    volumes:
      # Mount source code for hot-reload
      - ./packages/server/src:/app/packages/server/src
      - ./packages/server/package.json:/app/packages/server/package.json
      - ./packages/server/tsconfig.json:/app/packages/server/tsconfig.json
      # Mount root package.json for workspace configuration
      - ./package.json:/app/package.json
    environment:
      - NODE_ENV=development
      - PORT=3001
      - MONGODB_URI=mongodb://mongodb:27017/counter
    depends_on:
      mongodb:
        condition: service_healthy
    networks:
      - app-network

  app-static:
    build:
      context: .
      dockerfile: ./packages/app-static/Dockerfile.dev
    container_name: new-infra-test-app-static-dev
    ports:
      - "8080:3000"
    volumes:
      # Mount source code for hot-reload
      - ./packages/app-static/src:/app/packages/app-static/src
      - ./packages/app-static/package.json:/app/packages/app-static/package.json
      - ./packages/app-static/tsconfig.json:/app/packages/app-static/tsconfig.json
      - ./packages/app-static/build.ts:/app/packages/app-static/build.ts
      - ./packages/app-static/bunfig.toml:/app/packages/app-static/bunfig.toml
      - ./packages/app-static/components.json:/app/packages/app-static/components.json
      # Mount root package.json for workspace configuration
      - ./package.json:/app/package.json
    environment:
      - NODE_ENV=development
      - PORT=3000
      - API_URL=http://server:3001
    depends_on:
      - server
    networks:
      - app-network

volumes:
  mongodb-data-dev:

networks:
  app-network:
    driver: bridge

